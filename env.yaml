# This is a Spack Environment file.
#
# It describes a set of packages to be installed, along with
# configuration settings.
spack:
  # add package specs to the `specs` list
  specs:
    - mpich@4
    - parallel-netcdf
    #- hdf5@1.12.1+hl+mpi
    - netcdf-fortran@4.5.3
    #- lowfive@master
    - henson+python+mpi-wrappers build_type=Debug
      # - netcdf-c@4.8.1+mpi # added in create script in develop mode instead of here
      # - mpas-o-scorpio+hdf5 # added in create script in develop mode instead of here
      # - scorpio-example # added in create script in develop mode instead of here

  view: true

  concretizer:
    unify: true
